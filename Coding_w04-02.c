#include <stdio.h>            //ไม่มีการใช้ <> //ไม่มี # ด้านหน้า
int main() {        
   int a = 10;                //ไม่มีเครื่องหมาย ; 
    printf("a:= %d\n", a);    //ไม่มีเครื่องหมาย ; และไม่มี \n 
   return 0;                  //ไม่มีเครื่องหมาย ; 
}